---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "artifactkeeper_repository_npm_scope_policy Resource - artifactkeeper"
subcategory: ""
description: |-
  The npm scope allowlist for a repository: which @scope packages it may serve and whether unscoped packages are allowed. A singleton keyed by repository key: no create/delete (the policy lives with the repository), and writes replace the stored policy. Only meaningful for a remote member of an npm virtual repository; the backend rejects it otherwise.
---

# artifactkeeper_repository_npm_scope_policy (Resource)

The npm scope allowlist for a repository: which `@scope` packages it may serve and whether unscoped packages are allowed. A singleton keyed by repository key: no create/delete (the policy lives with the repository), and writes replace the stored policy. Only meaningful for a remote member of an npm virtual repository; the backend rejects it otherwise.

## Example Usage

```terraform
# Restrict an npm remote (a member of an npm virtual repo) to the @myorg scope,
# and disallow unscoped packages. Omit allowed_scopes to impose no restriction.
resource "artifactkeeper_repository_npm_scope_policy" "scoped" {
  repository_key = artifactkeeper_repository.npm_remote.key
  allowed_scopes = ["@myorg"]
  allow_unscoped = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `repository_key` (String) Key of the repository whose npm scope policy is managed. Changing this forces a new resource.

### Optional

- `allow_unscoped` (Boolean) Whether unscoped packages (those without an `@scope`) are allowed. Defaults to `false`.
- `allowed_scopes` (List of String) npm scopes the repository may serve, each starting with `@` (e.g. `@myorg`). Omit or leave empty to impose no scope restriction.

### Read-Only

- `active` (Boolean) Whether the stored policy actually restricts anything (i.e. it is enforced rather than a no-op).
- `id` (String) Resource identifier. Equal to `repository_key`, since the policy is a singleton per repository.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import artifactkeeper_repository_npm_scope_policy.scoped npm-remote
```
