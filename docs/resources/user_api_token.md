---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "artifactkeeper_user_api_token Resource - artifactkeeper"
subcategory: ""
description: |-
  Mints an API token for a specific user (POST /users/{id}/tokens), which an admin can do on someone else's behalf. Use artifactkeeper_api_token for the credential the provider itself authenticates with, and artifactkeeper_service_account_token for machine identities. Tokens are immutable; any change forces a new token, and the secret is returned only on creation.
---

# artifactkeeper_user_api_token (Resource)

Mints an API token for a specific user (`POST /users/{id}/tokens`), which an admin can do on someone else's behalf. Use `artifactkeeper_api_token` for the credential the provider itself authenticates with, and `artifactkeeper_service_account_token` for machine identities. Tokens are immutable; any change forces a new token, and the secret is returned only on creation.

## Example Usage

```terraform
# Mint a token for another user (admin-only). For the provider's own credential
# use artifactkeeper_api_token, and for machine identities use
# artifactkeeper_service_account_token.
resource "artifactkeeper_user_api_token" "release_bot_readonly" {
  user_id         = artifactkeeper_user.release_bot.id
  name            = "read-only artifact access"
  scopes          = ["read:artifacts", "read:repositories"]
  expires_in_days = 90
}

output "release_bot_token" {
  value     = artifactkeeper_user_api_token.release_bot_readonly.token
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name for the token.
- `scopes` (List of String) Token scopes. Required here (unlike `artifactkeeper_api_token`, this endpoint has no default). A non-admin caller cannot mint `*`, `admin`, `delete:*` or `write:users`, and a scoped credential cannot mint beyond its own scopes.
- `user_id` (String) UUID of the user the token belongs to. Minting for another user requires an admin credential. Changing this forces a new token.

### Optional

- `expires_in_days` (Number) Days until expiry (1-365). Omit for a non-expiring token.

### Read-Only

- `created_at` (String)
- `expires_at` (String)
- `id` (String) The ID of this resource.
- `last_used_at` (String)
- `token` (String, Sensitive) The plaintext token. Only known at creation; not recoverable later (and not populated on import).
- `token_prefix` (String)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# User API tokens import as "<user_id>/<token_id>". The secret is not recoverable,
# so `token` stays null after an import.
terraform import artifactkeeper_user_api_token.release_bot_readonly 9d4e2f7a-1c8b-4a35-b6e0-3f9a1c2d8e50/2b7c1d05-3e4f-4a6b-8c9d-0e1f2a3b4c5d
```
