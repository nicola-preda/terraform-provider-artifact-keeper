# Singleton: the import id is arbitrary (the settings always exist). Any value
# works; "system" is the conventional id this resource uses in state.
terraform import artifactkeeper_system_settings.this system
